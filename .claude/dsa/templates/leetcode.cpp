/* ------------------------------------------------------------------
 * Problem   :
 * Judge     : LeetCode
 * Link      :
 * Category  :
 * Technique :
 * Difficulty:
 * Status    : unsolved
 * Date      :
 * Insight   :
 * ------------------------------------------------------------------ */

/* NOTE: no `#define int long long int` here, unlike the Codeforces template.
 * LeetCode matches your method signature against its own, so `vector<int>`
 * must stay a real 32-bit int or the submission will not compile there.
 * Use `long long` explicitly where you need the width.
 *
 * Paste only the class into LeetCode. To test locally:
 *     g++ -std=c++14 -DLOCAL -O2 file.cpp -o $TEMP/a.exe && $TEMP/a.exe
 */

#include <bits/stdc++.h>
using namespace std;

class Solution {
public:

};

#ifdef LOCAL
int main() {
    Solution s;
    // call s.method(...) with the samples from the problem statement and
    // print the result; assert against the expected output where easy.
    return 0;
}
#endif
